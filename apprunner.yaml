version: 1.0
runtime: python3 
build:
  env:
    - name: DATABASE_URL
      value: "postgresql+psycopg2://postgres:h<XujEbv05fYaux*)u0|0MXI!)9~@database-2.cluster-c76q8cuqewgx.ap-northeast-1.rds.amazonaws.com:5432"
  commands:
    build:
      - pip install -r requirements.txt
      - pip install gunicorn
      - python manage.py migrate
run:
  command: gunicorn gettingstarted.wsgi
  network: 
    port: 80
