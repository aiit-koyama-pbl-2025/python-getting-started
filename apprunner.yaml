version: 1.0
runtime: python3 
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install gunicorn
      - python manage.py migrate
run:
  command: gunicorn gettingstarted.wsgi
  network: 
    port: 80
